参考：http://www.liaoxuefeng.com/

1、查看当前的版本状态
git status
一定需要细看 返回信息
2、git diff .查看没有add的文件
3、git log --pretty=oneline 
4、git reset --hard HEAD^ 回到前一个版本，HEAD^前一个版本，HEAD^^前2个版本（git reset --hard commit_id）
5、git reflog  查看操作的每一个命令

git log --graph --pretty=oneline --abbrev-commit

gitignore自动生成
https://www.gitignore.io/


$ git checkout -b dev从当前版本（工作区、暂存区、）的基础上创建一个新版本。



